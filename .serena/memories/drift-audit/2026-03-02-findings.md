# Drift Audit Findings — 2026-03-02

## Documentation vs Reality Gaps (CRITICAL — fix first)

### Rule files are stale:
- `architectural-invariants.md` lists 6 plugins → actual: 14
- `plugin-system.md` missing 3 hooks: onPipelineStart, onPipelineComplete, onSettingsChange
- `plugin-system.md` missing 2 ctx methods: getSettings, notifySettingsChange
- `agent-identity-state.md` says AgentConfig is "planned" → already in schema
- `data-flow.md` says activity persistence is "tech debt in sendToThread" → already refactored into activity plugin
- `data-flow.md` line numbers off by 13+
- CLAUDE.md UI components list: 14 listed, 20 actual (missing Command, Collapsible, AlertDialog, Select, Textarea, Sidebar)

### Actual 14 plugins (in registration order):
1. identity, 2. activity, 3. context, 4. discord, 5. web, 6. cron,
7. delegation, 8. validator, 9. metrics, 10. summarization, 11. auto-namer,
12. audit, 13. time, 14. project

## Feature Wiring Gaps

### Phase 4 Reflection — partially active but undocumented:
- scoreAndWriteMemory fires checkReflectionTrigger → runReflection (fire-and-forget)
- AgentConfig.reflectionEnabled is NOT checked — runs unconditionally
- REFLECTION memories retrieved but not prioritized over EPISODIC in header
- Docs say "PAUSED" but it's partially running

### Phase 5 Heartbeat — schema only, zero code:
- AgentConfig.heartbeatEnabled + heartbeatCron exist in schema
- No code reads them, no UI exposes them, no server action for AgentConfig

### Project-Agent gap:
- Projects and Agents are isolated (no relation)
- Thread can have agentId OR projectId but no interaction model

## Dead Code
- onCommand hook: no plugin implements it, steps 6-7 removed from pipeline
- run-command-hooks.ts: dead file with deprecation notice

## Style Drift
- Rule clarification: "one export per helper" means one FUNCTION export. Co-located type exports are fine.
- Only 3 files actually violate: delegation-loop.ts (2 fns), file-discovery.ts (2 fns), calculate-cost.ts (2 fns)
- All other style rules clean (arrow functions, kebab-case, test placement, imports)

## Fix Priority — ALL RESOLVED (2026-03-02)
1. ✅ Updated all `.claude/rules/` files to match current code reality
2. ✅ Wired AgentConfig.reflectionEnabled + memoryEnabled checks in identity plugin
3. ✅ Cleaned up onCommand dead code
4. ✅ Split 3 multi-function helper files
5. ✅ Added AgentConfig UI to edit-agent-form
6. ✅ Phase 5 scheduled tasks fully implemented (CronJob CRUD, MCP tool, one-shot, lazy thread creation)
