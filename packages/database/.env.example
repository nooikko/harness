# Database
DATABASE_URL="postgresql://user:password@localhost:5432/harness?schema=public"

# Qdrant vector search (optional — search degrades to FTS-only without it)
QDRANT_URL="http://localhost:6333"
